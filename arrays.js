var frutas = ["manzana", "pera", "papaya", "melon"]

// llamado en cosola al arreglo
console.log(frutas);

// cantidad de elementos de un arreglo
console.log(frutas.length);

// llamado en cosola un elemento del arreglo
//javascript inicia el conteo en 0
console.log(frutas[2]);

// agregar elementos al final del arreglo
var masFrutas =  frutas.push("uvas")

// eliminar ultimo elmento del arreglo
var masFrutas =  frutas.pop("uvas")

// agregar elementos al principio del arreglo
var masFrutas =  frutas.unshift("coco")

// elimina el primer elemento del arreglo
var masFrutas =  frutas.shift("coco")

// leimina el primer elemento del arreglo
varpositions = frutas.indexOf("melon")